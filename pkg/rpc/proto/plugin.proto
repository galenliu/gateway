syntax = "proto3";
package plugin;
import "message.proto";
option go_package = "../../rpc";

service PluginServer {
  // Sends another greeting
  rpc Register (stream plugin.PluginRegisterRequestMessage) returns (stream BaseMessage) {}
}

