syntax = "proto3";

package rpc;

option go_package = "../rpc";

service PluginServer {
  rpc AllSteam(stream BaseMessage) returns(stream BaseMessage){}
}

message BaseMessage {
  int32 messageType = 1;
  string adapterId = 2;
  string data = 3;
}

