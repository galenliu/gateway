* 下拉子模块

```shell
git submodule updata --init --recursive   
```

```shell
git submodule update --remote [子模块名称]
```

// 初始化本地子项目配置

```
$ git submodule init
```

// 拉取子仓库文件

```
$ git submodule update
```
