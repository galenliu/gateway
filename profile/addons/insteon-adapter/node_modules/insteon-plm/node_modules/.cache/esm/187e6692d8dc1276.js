let readFileSync,join;_d8d‍.x([["findProductInfo",()=>findProductInfo]]);_d8d‍.w("fs",[["readFileSync",["readFileSync"],function(v){readFileSync=v}]]);_d8d‍.w("path",[["join",["join"],function(v){join=v}]]);


const database = JSON.parse(readFileSync(join(__dirname, 'devices.json')));

       function findProductInfo(category, subcategory) {
  const categoryInfo = database[category];
  if (!categoryInfo) {
    return null;
  }

  let productInfo = categoryInfo[subcategory];
  if (!productInfo) {
    productInfo = categoryInfo[0];
  }

  return productInfo;
}
